version: '3'
services:
  api:
    container_name: tilanvarauspalvelu
    build:
      context: ./
      dockerfile: ./docker/api/Dockerfile
    command: python manage.py runserver 0:8000
    volumes:
      - .:/usr/src/app
    environment:
      - DEBUG=1
    ports:
      - "8000:8000"

