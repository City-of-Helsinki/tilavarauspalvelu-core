{{ price_label | safe }}: {% if price > 0 %}{{ price | currency }} € ({{ vat_included_label | safe }} {{ tax_percentage }} %){% else %}0 €{% endif %}
{{ booking_number_label | safe }}: {{ booking_number }}
