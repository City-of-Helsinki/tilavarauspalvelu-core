{
  "name": "tilavaraus",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "ncu": "npm-check-updates -p pnpm --deep --target minor --format group",
    "ncu:upgrade": "npm-check-updates -p pnpm --deep --target minor  --format group --upgrade",
    "clean": "npkill",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "lint:fix": "cd common && pnpm lint --fix && cd ../ui && pnpm lint --fix && cd ../admin-ui && pnpm lint --fix",
    "lint:css": "cd common && pnpm lint:css && cd ../ui && pnpm lint:css && cd ../admin-ui && pnpm lint:css",
    "test": "cd common && pnpm test && cd ../ui && pnpm test && cd ../admin-ui && pnpm test",
    "tsc": "cd common && pnpm tsc && cd ../ui && pnpm tsc && cd ../admin-ui && pnpm tsc",
    "generate-certificate": "mkdir -p certificates && echo \"[dn]\nCN=local-tilavaraus\n[req]\ndistinguished_name = dn\n[EXT]\nsubjectAltName=DNS:local-tilavaraus\nkeyUsage=digitalSignature\nextendedKeyUsage=serverAuth\"| openssl req -x509 -out certificates/local-tilavaraus.crt -keyout certificates/local-tilavaraus.key -newkey rsa:2048 -nodes -sha256 -subj '/CN=local-tilavaraus' -extensions EXT -config /dev/stdin"
  },
  "resolutions": {
    "graphql": "^16.6.0",
    "next": "13.1.1",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "npkill": "^0.10.0",
    "npm-check-updates": "^16.6.2",
    "turbo": "^1.10.13"
  }
}
